# dependencies
node_modules/

# next.js build output
.next/
out/

# env files (per-app .env stay local; examples are committed)
.env
.env.*
!.env.example

# misc
.DS_Store
*.log
coverage/
*.tsbuildinfo

# local git-history backups of the pre-monorepo app repos (history lives on GitHub)
_archive/git-backups/
