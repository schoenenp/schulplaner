# Used by the Prisma CLI (db:generate, db:migrate, db:push, db:studio, db:seed).
# Both apps talk to the same database; keep this in sync with the apps' .env.
DATABASE_URL="mysql://user:password@localhost:3306/schoolbooks"
